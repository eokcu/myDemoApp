<title>My application deployed automatically</title>
  <h1>Hello, World!</h1>
	<h2>This application takes two lists of integers and two strings then it returns a new string from the strings chars by indexes from lists of integers</h2>
	<p>In the first and second form, enter lists of integers separated by newline character. The integers must match the index of the regarding strings.
	It means that the integers value must be between 0(including) and the length of the regarding strings.</p>
	<p>In the third and fourth form, enter the strings.</p>
    Result is {{result}}


  <form action="compute" method="post">
        <textarea name="input1" rows="10" cols="30"></textarea>
        <textarea name="input2" rows="10" cols="30"></textarea>
	<textarea name="input3" rows="10" cols="30"></textarea>
        <textarea name="input4" rows="10" cols="30"></textarea>
        <br>
        <input type="submit">
  </form>
